# 5.1 Validation Checklist — Wire Path B On-Chain Proof Verification

Use this checklist to verify the 5.1 fix end-to-end.

## 1. Pre-merge code review

- [ ] `contracts/credential_registry/src/checks.rs` reduces the raw 32-byte ed25519 key with `U256::from_be_bytes(...).rem_euclid(&BN254_MODULUS)` before comparing to `pi.bound_address`.
- [ ] The BN254 modulus constant byte array matches `packages/zk-shared/src/field.ts` exactly.
- [ ] `apps/web/src/lib/stellar.ts` implements `isRootValid`, `isNullifierUsed`, `verifyProofOffchain`, and `submitRegister` (Path B). `submitVerifyAndRegister` remains stubbed (Path A).
- [ ] `verifyProofOffchain` uses `UltraHonkVerifierBackend` with `public/circuit/vk` and public inputs in circuit order: `root, scope, bound_address, nullifier, disclosed`.
- [ ] `/api/verify` accepts `boundStellarAddress` and rejects payloads containing `s`.
- [ ] `ProvePanel.tsx` posts `proof`, `publicInputs`, and `boundStellarAddress`.
- [ ] `verification.service.ts` passes `boundStellarAddress` to `submitRegister`.

## 2. Local test gate

```bash
cd contracts/credential_registry && cargo test
pnpm --filter @zelyo/web typecheck
pnpm --filter @zelyo/web test
pnpm --filter @zelyo/web lint   # only pre-existing MintForm warning allowed
```

- [ ] `cargo test` — 11 passed.
- [ ] `typecheck` — clean.
- [ ] `test` — 155 passed.
- [ ] `lint` — 0 errors (1 pre-existing warning OK).

## 3. Contract redeploy (required)

- [ ] Run `pnpm contracts:deploy`.
- [ ] Copy the new `CREDENTIAL_REGISTRY_CONTRACT_ID` into `.env` and Railway variables.
- [ ] Ensure `ISSUER_STELLAR_ACCOUNT` matches the deployer/attestor used by `scripts/deploy-contracts.ts`.
- [ ] Publish at least one Merkle root so `is_root_valid` returns `true`.

## 4. End-to-end proof verification (testnet)

- [ ] Use a holder whose **raw ed25519 key is ≥ BN254_P** to actually exercise the reduction fix.
- [ ] Mint a credential for that holder and publish the root.
- [ ] In the holder wallet, generate a proof bound to a Stellar address.
- [ ] **Expected:** ledger shows `SEALED ON-CHAIN <txHash>` instead of `REGISTRY REJECTED`.
- [ ] Open the explorer link and confirm only `nullifier`, `root`, and `bound_address` are recorded — no PII.
- [ ] Submit the same proof again and confirm `NULLIFIER_USED`.

## 5. Regression / reveal e2e specs

- [ ] Remove `.fixme` from `tests/e2e/reveals.spec.ts` §13.1, §13.2, §13.3.
- [ ] Run `pnpm test:e2e`.
- [ ] **Expected:** all three reveal specs pass.

## 6. Deployment hygiene

- [ ] `.env` / Railway `CREDENTIAL_REGISTRY_CONTRACT_ID` updated to the newly deployed contract.
- [ ] `VERIFIER_CONTRACT_ID` unchanged.
- [ ] Roots are re-published under the new contract, or new credentials are issued after redeploy.
